# Jump up N directories. ex: cd ../.. is the same as up 2

# Instructions
# Put the following function inside .bashrc
# source ~/.bashrc

function up {
ups=""
for i in $(seq 1 $1)
do
    ups=$ups"../"
done
cd $ups
}